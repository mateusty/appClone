import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        width: '100%',
        height: 56,
        backgroundColor: '#216DAE',
        borderRadius: 6,
        margin: 12,
        alignItems: 'center',
        justifyContent: 'center',
    },

    buttonText: {
        color: '#f0f0f0',
        fontSize: 20,
    }
})
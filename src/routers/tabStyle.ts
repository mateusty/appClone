import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    tabTopBar: {
        position: 'absolute',
        top: -10,
        borderRadius: 3,
        alignItems: 'center',
        height: 2,
        width: 56
    }
})